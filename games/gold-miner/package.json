{
  "name": "gold-miner",
  "version": "1.0.0",
  "description": "Classic Gold Miner game",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=build/index.js --format=esm --target=es2015",
    "dev": "esbuild src/main.ts --bundle --outfile=build/index.js --format=esm --target=es2015 --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "phaser": "^3.80.1",
    "@types/phaser": "^3.80.0"
  }
}